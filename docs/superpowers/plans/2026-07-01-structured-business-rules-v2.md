# 结构化业务规则文档脉路重构 — 实施计划

> 目标：将 `training_doc/知识点/结构化业务规则.md` 按 v2 设计规范重写为 5 章结构。

---

## Task 1: 重写文档为 5 章结构

**文件**：`training_doc/知识点/结构化业务规则.md`（覆盖写入）

### Step 1: 写入第 1 章 — PRD 天然描述增量

论证主干：
- PRD 天然是增量文档（v1.0→v1.2→v2.0），这不是缺陷而是协作模式决定
- 后果：当前全貌散落多份历史 PRD，无单一"当前有效"文档
- 示例：Agent 拿 v2.0 知"密码最少 8 位"，不知 v1.0 "邮箱不存在错误文案"
- 结论：需要常青 SSoT 文档

### Step 2: 写入第 2 章 — 不是所有需求都需要结构化

- 开头论断：只有业务规则需要结构化（逻辑密度高、错过即事故）
- 四种性质分类表（去掉"四象限"措辞）
- 判断标准：无法直观穷举 + 测试重心 → 该结构化

### Step 3: 写入第 3 章 — PRD 与业务规则的协同

**A. 全景定位**：
- 流转图：PRD → 业务规则 → SPEC → 测试
- 三者对比表
- 为什么不是三合一
- 三方 ID 引用联动

**B. 双轨协作**：
- PM 有全貌认知，缺规范化表达
- 双轨模型：Agent 提取变更 → 更新规则 → git diff
- PM 维护方式：直接编辑 / Agent 辅助
- PRD 引用方式：全貌链接 + 变更 diff
- 测试视角收束

### Step 4: 写入第 4 章 — 业务规则结构化实操指南

以 OKF 开头作为方法引子，然后：
1. 核心原则：一概念一文件 = SSoT
2. 目录结构：`business-rules/`
3. 粒度选择
4. YAML 文件头
5. 正文结构（流程 + 异常表 + AC）
6. 行内约束标记
7. Git 管理

### Step 5: 写入第 5 章 — 推行策略与常见难点

三个难点 + 渐进分级，内容基本复用 v1。

### Step 6: 验证

- [ ] 确认文档共 5 个顶级章节（## 标题 5 个）
- [ ] 确认无"三个盲区""四象限"等废弃措辞
- [ ] 确认"为什么 PM 需要专门维护全貌"已替换为规范化表达论述
- [ ] 确认 OKF 在第 4 章开头而非独立章
