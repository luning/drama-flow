# Claude Code 原理及使用技巧

## 目录

1. [感知到的工作过程](#感知到的工作过程)
2. [Agent 内部结构](#agent-内部结构)
   - [整体架构](#整体架构)
   - [推理过程中维护的临时认知结构](#推理过程中维护的临时认知结构)
3. [CLAUDE.md 配置原则](#claudemd-配置原则)
4. [高效工作流](#高效工作流)
   - [复杂任务用 Plan Mode](#复杂任务用-plan-mode)
   - [让 Claude 先采访你](#让-claude-先采访你)
   - [分阶段工作流](#分阶段工作流)
   - [小任务别用复杂工作流](#小任务别用复杂工作流)
5. [调试与纠错](#调试与纠错)
6. [上下文管理](#上下文管理)
   - [50% 时考虑手动压缩](#50-时考虑手动压缩)
   - [指定压缩策略](#指定压缩策略)
   - [Checkpoints（检查点）](#checkpoints检查点)
7. [Subagents（子智能体）](#subagents子智能体)
   - [为什么用 Subagent](#为什么用-subagent)
   - [调用方式](#调用方式)
   - [常用场景](#常用场景)
   - [生成—评估分离](#生成评估分离)
8. [Skills 与 Commands](#skills-与-commands)
   - [Skills（技能）](#skills技能)
   - [常用内置 Commands](#常用内置-commands)
   - [Skill vs Command 区别](#skill-vs-command-区别)
9. [权限与安全](#权限与安全)
   - [Hooks vs CLAUDE.md 选型](#hooks-vs-claudemd-选型)
   - [Allowlist 减少审批疲劳](#allowlist-减少审批疲劳)
   - [deny 比 Hooks 更安全](#deny-比-hooks-更安全)

---

## 感知到的工作过程

```
┌─────────────────────────────────────────────────────┐
│              Claude Code 执行循环                    │
│                                                     │
│  你的输入 + CLAUDE.md + 打开的文件                    │
│                  ↓                                  │
│         LLM 思考：下一步做什么？                       │
│                  ↓                                  │
│         调用工具（Tool Use）                         │
│         ├── Read   → 读文件内容                      │
│         ├── Edit   → 修改文件                       │
│         ├── Write  → 创建新文件                      │
│         └── Bash   → 运行命令（测试 / 编译）          │
│                  ↓                                 │
│         观察结果 → 再思考 → 再调用工具                 │
│                  ↓                                  │
│         完成 or 等待人工确认                          │
└─────────────────────────────────────────────────────┘
```

**几个关键点**

- CLAUDE.md 在每次对话开始时**自动注入到 system prompt**，这是约束持久化的底层机制
- 工具调用**可观测**：能看到读了什么、改了什么、运行了什么命令
- 模型基于工具返回结果决定下一步——所以测试失败信息要清晰
- 模型没有"记忆"，一切依赖**当前上下文窗口**

---

## Agent 内部结构

### 整体架构

```
┌──────────────────────────────────────────────────────────────────────┐
│                              用户 USER                                │
│                                                                      │
│   “修复支付问题” / “重构认证系统” / “分析整个代码库结构”                    │
└──────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
╔══════════════════════════════════════════════════════════════════════╗
║                         上下文操作系统 Context OS                      ║
║══════════════════════════════════════════════════════════════════════║
║                                                                      ║
║  1. 上下文组装 Context Assembly                                       ║
║     • 动态拼装 Prompt                                                 ║
║     • 选择相关文件                                                     ║
║     • 注入历史状态                                                     ║
║     • 构建当前任务工作区                                                ║
║                                                                      ║
║  2. 检索系统 Retrieval Engine                                         ║
║     • Grep / Symbol Search                                           ║
║     • Embedding Retrieval                                            ║
║     • 依赖关系分析 Dependency Graph                                    ║
║                                                                      ║
║  3. 记忆系统 Memory System                                            ║
║     • Session Memory（当前会话）                                      ║
║     • Task Memory（任务状态）                                         ║
║     • Project Memory（项目长期认知）                                   ║
║                                                                      ║
║  4. 注意力管理 Attention Management                                   ║
║     • Token Budgeting                                                ║
║     • Context Prioritization                                         ║
║     • 历史压缩 Context Compression                                    ║
║                                                                      ║
║  5. 工作区感知 Workspace Awareness                                    ║
║     • 项目结构理解                                                    ║
║     • 架构理解 Architecture Awareness                                 ║
║     • Git 状态感知                                                    ║
║                                                                      ║
║  6. 规范注入 Spec / Rules Injection                                   ║
║     • CLAUDE.md                                                      ║
║     • OpenSpec                                                       ║
║     • Coding Standards                                               ║
║     • 项目开发规则                                                     ║
║                                                                      ║
║  7. 推理状态 Reasoning State                                          ║
║     • Plan（计划）                                                    ║
║     • Todo（待办）                                                    ║
║     • Scratchpad（临时思考区）                                         ║
║     • Findings（当前发现）                                            ║
║     • Error Tracking（错误跟踪）                                      ║
║                                                                      ║
╚══════════════════════════════════════════════════════════════════════╝
                                  │
                                  ▼
╔══════════════════════════════════════════════════════════════════════╗
║                         推理循环 Reasoning Loop                       ║
║══════════════════════════════════════════════════════════════════════║
║                                                                      ║
║   Observe → Reason → Decide → Act → Observe → Replan                 ║
║                                                                      ║
║   观察 → 推理 → 决策 → 执行 → 再观察 → 重新规划                           ║
║                                                                      ║
║   模型持续决定：                                                       ║
║                                                                      ║
║   • 当前真正的问题是什么                                                ║
║   • 哪些上下文最重要                                                   ║
║   • 下一步该调用什么工具                                                ║
║   • 是否需要重新规划                                                   ║
║   • 是否需要压缩上下文                                                  ║
║   • 是否需要创建子任务                                                  ║
║                                                                      ║
╚══════════════════════════════════════════════════════════════════════╝
                                  │
                                  ▼
┌──────────────────────────────────────────────────────────────────────┐
│                         工具路由 Tool Router                          │
│                                                                      │
│   为当前步骤选择下一项可调用能力                                          │
│   Select next callable capability                                    │
└────────────────┬───────────────────────────────┬─────────────────────┘
                 │                               │
                 ▼                               ▼
     ┌──────────────────────────┐    ┌──────────────────────────┐
     │      内置工具 Builtin     │    │        MCP 工具 MCP       │
     ├──────────────────────────┤    ├──────────────────────────┤
     │ Read                     │    │ PostgreSQL               │
     │ Edit                     │    │ Browser                  │
     │ Write                    │    │ Figma                    │
     │ MultiEdit                │    │ Jira                     │
     │ Grep                     │    │ Slack                    │
     │ Glob                     │    │ Notion                   │
     │ LS                       │    │ AWS                      │
     │ Bash                     │    │ Kubernetes               │
     │ Git                      │    │ Internal APIs            │
     │ Task                     │    │ Redis                    │
     │ WebFetch                 │    │ CI/CD Systems            │
     └────────────┬─────────────┘    └────────────┬─────────────┘
                  │                               │
                  └──────────────┬────────────────┘
                                 ▼
┌──────────────────────────────────────────────────────────────────────┐
│                        工具接口 Tool Interface                        │
│                                                                      │
│   所有工具统一抽象为可调用能力                                            │
│   Unified callable tool abstraction                                  │
└────────────────────────────┬─────────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────────┐
│                        执行运行时 Execution Runtime                    │
│══════════════════════════════════════════════════════════════════════│
│                                                                      │
│   真正执行动作的环境                                                    │
│   Real execution environment                                         │
│                                                                      │
│   • Local Machine（本机）                                             │
│   • Docker Sandbox（Docker 沙箱）                                     │
│   • Remote VM（远程虚拟机）                                            │
│   • Browser Sandbox（浏览器沙箱）                                      │
│   • CI/CD Runner                                                     │
│   • Cloud Container（云容器）                                         │
│                                                                      │
│   Runtime 控制：                                                      │
│                                                                      │
│   • 文件系统 Filesystem                                               │
│   • 权限 Permissions                                                  │
│   • 网络 Networking                                                   │
│   • 安全隔离 Isolation                                                │
│   • 资源限制 Resource Limits                                          │
│                                                                      │
└────────────────────────────┬─────────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────────┐
│                          真实世界 Real World                          │
│══════════════════════════════════════════════════════════════════════│
│                                                                      │
│   Files / Git / DB / Browser / APIs / Docker / Tests                 │
│                                                                      │
│   文件 / Git / 数据库 / 浏览器 / API / 容器 / 测试系统                    │
│                                                                      │
└────────────────────────────┬─────────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────────┐
│                           观察结果 Observation                        │
│══════════════════════════════════════════════════════════════════════│
│                                                                      │
│   • 文件内容 File Contents                                            │
│   • 日志 Logs                                                        │
│   • 错误 Errors                                                      │
│   • 测试结果 Test Results                                             │
│   • Git Diff                                                         │
│   • Browser Snapshot                                                 │
│   • Query Results                                                    │
│                                                                      │
│   所有结果重新进入 Context OS                                           │
│   All outputs flow back into Context OS                              │
│                                                                      │
└────────────────────────────┬─────────────────────────────────────────┘
                             │
                             ▼
                    Back Into Reasoning Loop
                           回到推理循环
```

### 推理过程中维护的临时认知结构

```
                USER GOAL
                     │
                     ▼
         ┌──────────────────────┐
         │  Claude Reasoning    │
         └──────────┬───────────┘
                    │
                    ▼
      ┌──────────────────────────────┐
      │ Internal Cognitive State     │
      ├──────────────────────────────┤
      │                              │
      │ PLAN                         │
      │ 1. Analyze auth flow         │
      │ 2. Locate token logic        │
      │ 3. Patch middleware          │
      │                              │
      │ TODO                         │
      │ [ ] Read auth service        │
      │ [ ] Run tests                │
      │ [ ] Check logs               │
      │                              │
      │ SUBTASKS                     │
      │ - JWT analysis               │
      │ - Session handling           │
      │                              │
      └──────────────────────────────┘
```


## CLAUDE.md 配置原则

- **保持简短**：控制在 60 行以内，硬上限 300 行
- **只放 Claude 可能忽略的信息**：构建命令、测试命令、分支规范、架构决策
- **能从代码推断的内容不要写**：语言语法、框架 API 等不需要
- **规则太多？拆分到 `.claude/rules/`**，按需加载，不污染主文件
- **关键规则用 XML 标签包裹**，防止被忽略

好的 CLAUDE.md 结构示例：
```
## 工作流
- 每次代码变更后运行 `npm test`
- 使用 Conventional Commits（feat:, fix:, refactor:, docs:）
- 每次提交前运行 `eslint . --fix`
## 技术栈
- Node.js 18+, Express 4.x, PostgreSQL 16
- 测试：Jest + React Testing Library
```

---

## 高效工作流

### 复杂任务用 Plan Mode
- 按 Shift+Tab 两次进入计划模式 → Claude 只研究不写代码
- 确认计划后再切换回正常模式执行
- 推荐流程：探索 → 规划 → 实现 → 提交

### 让 Claude 先采访你
- 给出简单需求，让 Claude 用 AskUserQuestion 采访你
- 它能发现你忽略的边缘情况
- 采访后建议开新会话执行（采访对话会污染上下文）

### 分阶段工作流
- 理解代码库 → 修改；先规划 → 再实现；生成 → 验证
- 不要把所有步骤压缩到一个大提示词里

### 小任务别用复杂工作流
- 3-5 分钟能完成的事，直接说
- 重命名变量这种小事，一句话就行

---

## 调试与纠错

1. **粘贴 bug，说 "fix"**
   - 把错误信息粘贴给 Claude，说一个字："fix"
   - 不要指导怎么修、不要猜测原因、不要指定解决方案
   - 直接让 Claude 修的成功率 80%+

2. **两次失败 = /clear**
   - 同一个问题修正超过两次，`/clear` 重新开始
   - 上下文污染会降低性能

3. **走偏了？Esc Esc 回滚**
   - 按两次 Esc（或 `/rewind`）直接回滚到上一个检查点
   - 同一问题偏差两次？`/clear` 重启

4. **要求重写平庸方案**
   - 当 Claude 给出能工作但不优雅的解决方案时，说：
   - "知道你现在知道的一切，抛弃这个，实现优雅的解决方案"
   - 重写版本通常比修补版本好得多

5. **警惕三种典型反模式**
   - **过早放弃**：说"大部分实现了但 XX 不工作"时，把任务拆更小
   - **修改测试凑代码**：严格审查测试变更，确保测试在验证行为而非匹配 bug
   - **重写不删旧代码**：审查 diff 时确认旧实现已被替换，不是追加

---

## 上下文管理

### 50% 时考虑手动压缩
- 上下文使用超过 60-70% 时，性能明显下降
- **在 50% 时考虑手动执行 `/compact`**，不要等自动压缩
- 用 `/statusline` 实时监控使用情况

### 指定压缩策略
```
/compact focusing on API changes     # 聚焦 API 变更
/compact keep test-related history   # 保留测试相关
/compact keep error resolution       # 保留错误解决
```

### Checkpoints（检查点）
- 每次 Claude 操作自动创建，可独立回滚对话或代码
- 跨会话持久化，但不是 git 的替代品

---

## Subagents（子智能体）

### 为什么用 Subagent

子智能体解决三个问题：**上下文隔离**（中间过程不污染主对话）、**并行加速**（多个子智能体同时工作）、**专用提效**（不同子智能体类型针对性优化，输出质量高于通用 mega-agent）。

### 调用方式

- **自然语言**：在对话中说明意图，Claude 自行判断是否拆分。如"用 subagent 同时检查 android/ 和 h5/ 两个模块的安全问题"
- **显式 Agent 工具**：通过 `Agent` 工具指定子智能体类型和参数，可精确控制
- **后台运行**：设置 `run_in_background: true`，完成后自动通知

### 常用场景

| 场景 | 推荐类型 | 说明 |
|------|---------|------|
| 代码库探索 | `Explore` | 比 grep 更智能，自动组合多种搜索策略 |
| 跨文件重构 | `general-purpose` × N | 拆分到多模块，各自搜索替换 |
| 代码审查 | `cr-refactor` skill | 并行检查多个文件 |
| 安全审查 | `security-review` skill | 独立上下文检查凭据泄露、注入风险 |
| 方案设计 | `Plan` | 设计完回到主对话执行 |
| 补充测试 | `spec-validate` skill | 按模块拆分 AC 覆盖检查 |
| **独立评估** | `general-purpose`（干净上下文） | 生成完成后主动触发，避免自评估偏差 |

> **原则**：任务可拆分、各子任务独立无依赖 → 适合 Subagent。高度耦合、需来回确认细节 → 主对话处理。

### 生成—评估分离

**同一个 Agent 生成代码后再审查自己的实现，存在系统性偏差**——它对已走过的实现路径有"信念加成"，倾向于跳过真正的边界条件，给出"看起来合理"的自评。

解法是在生成阶段结束后，**主动触发一个独立上下文的 subagent**，它没有参与过实现过程，只看最终代码。

**触发方式**，生成或重构完成后直接说：

```
用独立 subagent 审查刚才的变更，角色是挑剔的代码审查者。
只看最终 diff，不看我们的实现过程。
重点：边界条件、错误处理、安全风险、与 SPEC.md AC 逐条对齐。
```

对于高频场景可以封装成 Skill（如 `cr-refactor`），避免每次手写 prompt。

**适用时机**：
- 复杂功能实现完成后（在自验之外加一层独立视角）
- 安全敏感代码（鉴权、支付流程、数据删除）
- 重构前后行为等价性验证

---

## Skills 与 Commands

### Skills（技能）

Skills 是将领域规则和工具打包成的可复用模块，调用方式：`/skill-name`。本课程中的 `rebuild-deploy`、`spec-validate`、`cr-refactor` 都是 Skill。

**目录结构**：
```
.claude/skills/skill-name/
  SKILL.md       # 主文件（Claude 优先读取）
  references/    # 参考资料（渐进式披露，按需读取，可选）
  scripts/       # 辅助脚本（固定逻辑，无需 AI 推理的部分，可选）
  examples/      # 示例代码（可选）
```

**核心设计原则**（详见 [Skills设计.md](Skills设计.md)）：
- **参数最小化**：让 Skill 自己从上下文推断，减少调用者出错
- **幂等性**：可重复调用，不产生副作用
- **结构化反馈**：stdout 输出让 Agent 可自修复
- **固定逻辑剥离**：无需推理的流程用脚本实现，Skill 聚焦推理判断

### 常用内置 Commands

| 命令 | 作用 | 使用时机 |
|------|------|---------|
| `/compact` | 压缩上下文 | 上下文使用超过 50% 时 |
| `/clear` | 清空会话重启 | 同一问题失败两次 |
| `/rewind` 或 `Esc Esc` | 回滚到上一检查点 | 发现走偏时立即回滚 |
| `/status` | 查看当前状态 | 随时检查上下文和文件状态 |
| `/plan` (Shift+Tab x2) | 进入计划模式 | 复杂任务先规划后执行 |

### Skill vs Command 区别

- **Skill**：领域知识包，适用于专业场景（代码审查、部署）。CLI Skill vs MCP Server 选型见 [Skills设计.md](Skills设计.md)
- **Command**：内置快捷操作（压缩、回滚），通用控制

---

## 权限与安全

### Hooks vs CLAUDE.md 选型

| 需求 | 推荐 | 原因 |
|------|------|------|
| 文件保存后自动 lint | Hook | 每次必须执行 |
| 阻止写入敏感文件 | Hook | 安全不能妥协 |
| 代码规范遵循 | CLAUDE.md | 需要情境判断 |
| API 命名规则 | CLAUDE.md | 存在例外模式 |

### Allowlist 减少审批疲劳
```json
{
  "permissions": {
    "allow": [
      "Bash(npm run lint:*)",
      "Bash(npm run test:*)",
      "Bash(git status)",
      "Read",
      "Glob",
      "Grep"
    ]
  }
}
```

### deny 比 Hooks 更安全
```json
{
  "permissions": {
    "deny": [
      "Read(./.env)",
      "Read(./.env.*)",
      "Read(./secrets/**)",
      "Bash(curl:*)"
    ]
  }
}
```
权限评估顺序：deny → ask → allow。设为 deny 后文件对 Claude "不可见"。

