# DramaFlow Kano 优先级分类

## 分类说明

| 分类 | 含义 | 策略 |
|------|------|------|
| M (Must-be) | 基本型需求，不做用户无法接受 | 必须实现，P0 |
| O (One-dimensional) | 期望型需求，做得越好满意度越高 | 核心体验，P0-P1 |
| A (Attractive) | 兴奋型需求，超出用户预期 | 差异化加分项，P2 |
| I (Indifferent) | 无差异需求，用户不关注 | 暂缓 |

---

## 一、Must-be 基本型（不做就没法用）

缺失这些功能，用户无法完成核心任务，产品不可用。

| 分类 | 功能 | 迭代 |
|------|------|------|
| M | 用户注册（AUTH-01） | 迭代 1 |
| M | 用户登录（AUTH-02） | 迭代 1 |
| M | 分类 Tab 切换（HOME-02） | 迭代 1 |
| M | 剧集列表展示（HOME-03） | 迭代 1 |
| M | 剧集详情页（DTL-01） | 迭代 2 |
| M | ExoPlayer 视频播放器（PLY-01） | 迭代 2 |
| M | 播放进度控制条（PLY-02） | 迭代 2 |
| M | Drama/Episode/User/WatchRecord 数据模型（DATA-01~04） | 迭代 1-2 |
| M | 测试数据 seed 脚本（DATA-05） | 迭代 1 |

## 二、One-dimensional 期望型（做得越好满意度越高）

这些功能直接影响用户体验质量，是竞争差异化的核心领域。

| 分类 | 功能 | 迭代 |
|------|------|------|
| O | 用户登出（AUTH-03） | 迭代 1 |
| O | 记住我 - Token 持久化（AUTH-05） | 迭代 3 |
| O | JWT Token 自动刷新（AUTH-06） | 迭代 3 |
| O | Banner 轮播图（HOME-01） | 迭代 1 |
| O | 数据分页加载（HOME-05） | 迭代 1 |
| O | 横屏全屏播放（PLY-03） | 迭代 2 |
| O | 倍速播放控制（PLY-05） | 迭代 3 |
| O | 播放记录读写（PROG-01/02） | 迭代 2 |
| O | 断点续播提示 UI（PROG-03） | 迭代 2 |
| O | 个性化推荐（REC-01） | 迭代 3 |

## 三、Attractive 兴奋型（超出预期，引发好评）

这些功能是惊喜点，用户没想到但看到后会特别喜欢。

| 分类 | 功能 | 迭代 |
|------|------|------|
| A | 第三方 OAuth 登录（AUTH-04） | 迭代 1 可选 |
| A | 骨架屏加载效果（HOME-04） | 迭代 1 可选 |
| A | 用户评论区（DTL-02） | 迭代 2 可选 |
| A | 手势快进/快退（PLY-04） | 迭代 2 可选 |
| A | 画质切换（PLY-06） | 迭代 3 可选 |
| A | 跨设备进度同步（DATA-06） | 迭代 3 可选 |
| A | 首页 A/B 测试开关（REC-02） | 迭代 3 可选 |

## 四、迭代交付路线图

```
迭代 1 （基本可用）
├── M: 注册/登录 + 分类Tab + 剧集列表 + 数据模型
├── O: Banner轮播 + 登出 + 分页加载
└── A: (可选) OAuth登录 + 骨架屏

迭代 2 （核心体验）
├── M: 剧集详情 + 视频播放 + 进度控制条
├── O: 播放记录读写 + 断点续播 + 横屏全屏
└── A: (可选) 评论区 + 手势快进

迭代 3 （体验优化）
├── O: 记住我 + Token刷新 + 倍速播放 + 个性化推荐
└── A: (可选) 画质切换 + 跨设备同步 + A/B测试
```

## 五、按 Kano 分类的优先级映射

| Kano 分类 | 研发优先级 | 质量要求 |
|-----------|----------|---------|
| M | P0 — 必须实现 | 高可靠性，不允许缺失 |
| O | P0~P1 — 核心体验 | 良好体验，持续优化 |
| A | P2 — 锦上添花 | 有最好，没有不影响核心 |
