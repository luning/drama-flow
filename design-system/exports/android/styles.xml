<?xml version="1.0" encoding="utf-8"?>
<!--
  styles.xml — Android Component Styles

  AUTO-GENERATED by scripts/design-system/generate_android.py
  from components/components.yaml.
  DO NOT EDIT MANUALLY. Modify components/components.yaml instead.
-->
<resources>
  <!-- ── Buttons ─────────────────────────────────────────────── -->

  <!-- 主操作按钮，每页最多一个 -->
  <style name="DramaFlow.Btn.Primary" parent="Widget.MaterialComponents.Button">
    <item name="android:padding">@color/spacing_md</item>
    <item name="android:minHeight">48px</item>
    <item name="cornerRadius">@color/radius_btn</item>
  </style>

  <!-- 次要操作按钮 -->
  <style name="DramaFlow.Btn.Secondary" parent="Widget.MaterialComponents.Button">
  </style>

  <!-- 轮廓按钮（收藏、关注等） -->
  <style name="DramaFlow.Btn.Outline" parent="Widget.MaterialComponents.Button.OutlinedButton">
    <item name="strokeColor">1.5px solid @color/color_primary</item>
  </style>

  <!-- 小按钮（标签、筛选等） -->
  <style name="DramaFlow.Btn.Sm" parent="Widget.MaterialComponents.Button">
    <item name="android:padding">@color/spacing_sm</item>
    <item name="android:minHeight">32px</item>
    <item name="cornerRadius">@color/radius_md</item>
  </style>

  <!-- 剧集卡片，用于推荐列表和搜索结果 -->
  <style name="DramaFlow.Drama.Card" parent="Widget.MaterialComponents.CardView">
    <item name="cornerRadius">@color/radius_card</item>
  </style>

  <!-- 继续观看卡片，显示进度条和上次观看信息 -->
  <style name="DramaFlow.Continue.Watching.Card" parent="Widget.MaterialComponents.CardView">
    <item name="cornerRadius">@color/radius_card</item>
    <item name="android:padding">@color/spacing_md</item>
    <item name="strokeColor">1px solid @color/color_primary_tint</item>
  </style>

  <!-- 表单输入框（邮箱、密码、搜索） -->
  <style name="DramaFlow.Form.Input" parent="Widget.MaterialComponents.Button">
    <item name="android:padding">@color/spacing_md</item>
    <item name="cornerRadius">@color/radius_btn</item>
    <item name="strokeColor">1.5px solid @color/border_default</item>
    <item name="android:minHeight">48px</item>
  </style>

  <!-- 分类标签 / 状态标签 -->
  <style name="DramaFlow.Tag" parent="Widget.MaterialComponents.Button">
    <item name="android:padding">@color/spacing_xs</item>
    <item name="cornerRadius">14px</item>
  </style>

  <!-- 进度条 -->
  <style name="DramaFlow.Progress.Bar" parent="Widget.MaterialComponents.Button">
    <item name="android:minHeight">4px</item>
    <item name="cornerRadius">2px</item>
  </style>

  <!-- 底部导航栏（Android 原生实现，H5 由 Android WebView 壳提供） -->
  <style name="DramaFlow.Bottom.Nav" parent="Widget.MaterialComponents.Button">
    <item name="android:minHeight">@color/device_bottom_nav_height</item>
  </style>

  <!-- 操作反馈 Toast（底部浮现，1.8s 消失） -->
  <style name="DramaFlow.Toast" parent="Widget.MaterialComponents.Button">
    <item name="android:padding">@color/spacing_md</item>
    <item name="cornerRadius">10px</item>
  </style>

  <!-- 骨架屏加载占位 -->
  <style name="DramaFlow.Skeleton" parent="Widget.MaterialComponents.Button">
    <item name="cornerRadius">@color/radius_sm</item>
  </style>

  <!-- 首页 Banner 轮播，16:7 比例 -->
  <style name="DramaFlow.Banner.Carousel" parent="Widget.MaterialComponents.Button">
    <item name="cornerRadius">@color/radius_xl</item>
  </style>

  <!-- 分类标签栏（横向滚动） -->
  <style name="DramaFlow.Category.Tabs" parent="Widget.MaterialComponents.Button">
    <item name="android:padding">@color/spacing_md</item>
  </style>

  <!-- 剧集列表项 -->
  <style name="DramaFlow.Episode.Item" parent="Widget.MaterialComponents.Button">
    <item name="android:padding">@color/spacing_md</item>
  </style>
</resources>
